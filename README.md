# TS_Playwright

This repository contains a **Playwright Test automation project using TypeScript** for web UI testing.  
It demonstrates both **inline tests** and **Page Object Model (POM)** style tests using Playwright’s built-in test runner and features.

## 🧰 Project Overview

This project was bootstrapped using `npm init playwright@latest`, giving you a working Playwright Test environment including:
- TypeScript support
- Example test in `tests/example.spec.ts`
- Playwright configuration
- GitHub Actions CI workflow to run tests automatically

Current tests include:
- SauceDemo login validation (inline + POM planned)
- Placeholder for additional practice flows (e.g., ParaBank)

## 📦 Getting Started

### Prerequisites

- Node.js (LTS version, e.g. Node 18+)
- npm (comes bundled with Node.js)
- Visual Studio Code or your preferred editor

### Install Dependencies

```bash
npm install
